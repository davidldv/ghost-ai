# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

Feature 14 — Node resize + inline label editing

## Current Goal

Complete.

## Completed

- Feature 01: Design System — shadcn/ui 4.5.0 installed, components (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea) added, lucide-react 1.14.0 installed, lib/utils.ts cn() helper created, globals.css dark-only theme (project tokens + shadcn dark vars in :root, no .dark switching).
- Feature 02: Editor Chrome — EditorNavbar (fixed top bar, sidebar toggle with PanelLeftOpen/PanelLeftClose, dark bg + bottom border) and ProjectSidebar (fixed overlay, slides in from left, isOpen/onClose props, Tabs for My Projects + Shared with empty states, New Project button) created in components/editor/.
- Feature 03: Authentication — NextAuth v5 (beta) + @auth/prisma-adapter, DB-backed sessions, GitHub + Google providers wired. Prisma 7.8.0 schema (User/Account/Session/VerificationToken) migrated to Supabase Postgres via @prisma/adapter-pg. lib/prisma.ts singleton, lib/auth.ts NextAuth config exporting handlers/auth/signIn/signOut, app/api/auth/[...nextauth]/route.ts handler. proxy.ts at root protects /project/*, /canvas/*, /editor/* — redirects unauth'd to /sign-in?callbackUrl=. /sign-in and /sign-up pages built with components/auth/AuthShell (two-panel large screens, form-only small) + AuthForm (Google + GitHub OAuth buttons, email form stub for future magic link). Root / redirects authed→/editor, unauthed→/sign-in. Email/magic-link provider deferred (no SMTP yet — form will fail silently until provider added). bun run build passes.
- Feature 04: Project Dialogs + Editor Home — `/editor` route built (client page composing EditorNavbar + ProjectSidebar + dialogs). Centered home content (heading "Create a project or open an existing one", description, "New Project" button with Plus icon, no card wrapper). `useProjectActions` hook now mock-only: owns ProjectRow[] state (initial mock data: 2 owned + 1 shared), filters into ownedProjects/sharedProjects, manages dialog/form/loading state, exposes openCreate/openRename/openDelete/close/handleNameChange/submit and live slugPreview (`<slug>-<4charSuffix>`). All fetch calls removed. Three dialogs in components/editor/dialogs/: CreateProjectDialog (name input, live `/editor/<slug>` preview, Enter submits via form), RenameProjectDialog (autoFocus prefilled input, current name in description, Enter submits, Save disabled when unchanged), DeleteProjectDialog (no input, destructive variant button, confirm-only). Sidebar rename/delete actions only render for owned projects (props undef on shared tab). Backdrop scrim + outside-click already handled by ProjectSidebar.
- Feature 05: Prisma project models — multi-file schema (prisma.config.ts `schema: "prisma"`). prisma/models/project.prisma adds `ProjectStatus` enum (DRAFT/ARCHIVED), `Project` (id, ownerId→User cascade, name, description?, status, canvasJsonPath?, createdAt/updatedAt; @@index ownerId, @@index createdAt), `ProjectCollaborator` (projectId cascade, email, createdAt; @@unique [projectId,email], @@index email, @@index [projectId,createdAt]). User model gained `projects Project[]` back-relation. lib/prisma.ts now branches on DATABASE_URL: `prisma+postgres://` → `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())`, else `PrismaPg` adapter. Cached on globalThis in dev. Migration `20260502021401_add_projects` applied. @prisma/extension-accelerate@3.0.1 installed. Build passes.
- Feature 06: Project API routes — backend-only REST endpoints. `app/api/projects/route.ts`: `GET` lists current user's projects (orderBy createdAt desc), `POST` creates project (defaults missing/blank name to `Untitled Project`, ownerId from session). `app/api/projects/[projectId]/route.ts`: `PATCH` rename (400 on invalid name), `DELETE`. All routes return `401` when unauthenticated; mutations return `403` for non-owner, `404` if project missing. Auth via `auth()` from `@/lib/auth`. lib/prisma.ts simplified to a single `PrismaClient` type (cast accelerate-extended client to PrismaClient so model accessors stay typed). UI wiring deferred. `bun run build` passes.
- Feature 07: Wire editor home + dialogs to project API. `POST /api/projects` now also accepts an optional `id` (validated `^[a-z0-9-]{3,80}$`, 409 on conflict) so client-generated `<slug>-<4charSuffix>` becomes the DB id and Liveblocks room id. New `lib/projects.ts` (`server-only`) `getEditorProjects()` returns `{ ownedProjects, sharedProjects }` — owned = `Project.ownerId == session.user.id`, shared = `ProjectCollaborator.email == session.user.email` excluding owned, both `orderBy createdAt desc`. `app/editor/page.tsx` is now a server component that calls the helper and passes lists to a new client wrapper `app/editor/editor-home.tsx` (composes EditorNavbar + ProjectSidebar + dialogs). `useProjectActions` no longer holds project state or mocks: it owns dialog/form state + `slugPreview` and calls `POST /api/projects` (then `router.push('/editor/<id>')` + refresh), `PATCH /api/projects/[id]` (then refresh), `DELETE /api/projects/[id]` (push `/editor` if `activeProjectId` matches deleted id, else refresh). Optional `activeProjectId` arg supports the future workspace route. `bun run build` passes.
- Feature 08: Editor workspace shell. `lib/project-access.ts` (`server-only`) exports `getCurrentIdentity()` (NextAuth session → `{ userId, email }`) and `checkProjectAccess(projectId, userId, email)` (owner or collaborator check). `components/editor/access-denied.tsx` — centered lock icon + back link to `/editor`. `app/editor/[roomId]/page.tsx` server component: unauthenticated → redirect `/sign-in`, missing/unauthorized project → `AccessDenied`, otherwise renders `WorkspaceShell`. `app/editor/[roomId]/workspace-shell.tsx` client wrapper composing EditorNavbar (project name centered, Share + AI toggle buttons), ProjectSidebar (activeProjectId highlighted), canvas placeholder, collapsible AI sidebar placeholder, project dialogs, ShareDialog. `EditorNavbar` updated with optional `projectName`, `onShare`, `onAiToggle`, `isAiOpen` props. `bun run build` passes.
- Feature 09: Share dialog. `GET /api/projects/[projectId]/collaborators` returns collaborators merged with User `name`/`image` from DB (lookup by email). `POST` invites by email (owner only, 409 on duplicate). `DELETE /api/projects/[projectId]/collaborators/[collaboratorEmail]` removes (owner only). `components/editor/dialogs/share-dialog.tsx`: owner view has email invite form + collaborator list with remove buttons + copy-link button with "Copied!" feedback; collaborator view is read-only list. `bun run build` passes.
- Feature 10: Liveblocks realtime infrastructure. `@liveblocks/client@3.18.5` and `@liveblocks/node@3.18.5` installed. `liveblocks.config.ts` at project root augments global `Liveblocks` interface with `Presence` (`cursor: {x,y}|null`, `isThinking: boolean`) and `UserMeta` (`id: string`, `info: { name, avatar, color }`). `lib/liveblocks.ts` lazy-initializes the Liveblocks node client on first call (avoids build-time secret validation) and exports `getUserColor(userId)` — deterministic color from 8-color palette via hash. `POST /api/liveblocks-auth` requires auth, verifies project access via `checkProjectAccess`, ensures room exists (creates if missing), returns session token with `name`/`avatar`/`color` metadata. `bun run build` passes.
- Feature 11: Base canvas. Installed `@liveblocks/react`, `@liveblocks/react-flow`, `@xyflow/react@^12`. New `types/canvas.ts` defines `CANVAS_NODE_TYPE`/`CANVAS_EDGE_TYPE` literals, `NodeShape` union (rectangle/diamond/circle/pill/cylinder/hexagon), `NODE_SHAPES` array, `NODE_COLORS` palette (8 fill+text pairs from ui-context), `DEFAULT_NODE_COLOR='neutral'`, `DEFAULT_EDGE_COLOR='#f8fafc'`, `CanvasNodeData` (label/color/shape), `CanvasNode`/`CanvasEdge` typed aliases, `SHAPE_DEFAULT_SIZES` (per-shape w/h), `ShapeDragPayload`, `SHAPE_DRAG_MIME`, `NODE_MIN_WIDTH=80`, `NODE_MIN_HEIGHT=50`. `components/canvas/canvas-room.tsx` is a client wrapper composing `LiveblocksProvider` (authEndpoint `/api/liveblocks-auth`), `RoomProvider` (`initialPresence={cursor:null,isThinking:false}`), `ClientSideSuspense` (loading fallback), and a class-based error boundary that surfaces connection failures. `components/canvas/canvas.tsx` wraps `ReactFlow` in a `ReactFlowProvider`, calls `useLiveblocksFlow<CanvasNodeT, Edge>({suspense:true})`, registers the `canvasNode` custom type, sets `connectionMode={Loose}`, dot-pattern `Background`, `MiniMap` (pannable/zoomable, dark mask), `defaultEdgeOptions` (smoothstep + arrow marker, `#f8fafc`), `fitView`, and `colorMode="dark"`. Workspace placeholder swapped for `<CanvasRoom roomId={projectId}/>`.
- Feature 12: Shape panel + drop. `components/canvas/shape-panel.tsx` renders a floating bottom-center pill toolbar with 6 native-draggable lucide icon buttons (rectangle/diamond/circle/pill/cylinder/hexagon). On `dragstart` it sets a `SHAPE_DRAG_MIME` payload `{shape,width,height}` from `SHAPE_DEFAULT_SIZES` and assigns a custom `setDragImage`. Canvas adds `onDragOver` (only handles its own MIME, sets `dropEffect='copy'`) and `onDrop` which converts client coords via `screenToFlowPosition`, centers the new node on the cursor, and dispatches a `{type:'add', item}` change to `onNodesChange` so Liveblocks syncs it. Each new node id is `${shape}-${base36(timestamp)}-${counter}`.
- Feature 13: Shape rendering + drag preview. `components/canvas/canvas-node.tsx` renders shape-specific backgrounds: rectangle/pill/circle use CSS (rounded-xl or full-radius div); diamond/hexagon/cylinder use inline SVG with `viewBox="0 0 100 100"` + `preserveAspectRatio="none"` and `vector-effect="non-scaling-stroke"` so strokes stay crisp at any node size. Border is subtle at rest (`#2a2a30`) and brightens to the node text color when selected. 4 stroke-bordered hover-only handles (top/right/bottom/left). Shape panel builds a per-shape DOM ghost preview (CSS for round shapes, inline SVG for polygons) and registers it via `setDragImage`, removing the element on `dragend`.
- Feature 14: Node resize + label editing. Selected nodes show a `NodeResizer` from `@xyflow/react` with `minWidth={NODE_MIN_WIDTH}`, `minHeight={NODE_MIN_HEIGHT}`, custom subtle handle/line classes. Double-clicking a node opens an inline `<textarea>` overlay positioned over the centered label area; it autofocuses, updates draft state on input, commits to node data via `useReactFlow().updateNodeData(id,{label})` on blur, and cancels (restoring previous label) on `Escape`. The textarea uses `nodrag nopan nowheel` classes plus stopped pointer/click events so editing never triggers React Flow drag/pan. Empty labels show a faint italic "Double-click to edit" placeholder centered in the node. `bun run build` passes.

## In Progress

## Next Up

- Add SMTP/Email provider when ready (Resend/Nodemailer) — then wire `Email` provider in lib/auth.ts.
- Set `LIVEBLOCKS_SECRET_KEY` in `.env` so the auth route + canvas can connect at runtime.
- AI sidebar implementation (replace placeholder with chat UI + tool integration).
- Live cursors + presence avatars on the canvas (use `<Cursors>` from `@liveblocks/react-flow` and `useOthers`).
- Persistence of canvas snapshots to Vercel Blob (per architecture-context).

## Open Questions

- SMTP provider for magic links (Resend? Postmark? raw SMTP?) — deferred per user.

## Architecture Decisions

- shadcn/ui over Tailwind v4 (CSS-based token config via @theme inline in globals.css, no tailwind.config.js).
- Dark-only theme: all shadcn :root variables set to dark values directly — no .dark class switching.
- Do not modify generated components/ui/* files after shadcn installation.
- Next.js 16 uses proxy.ts (not middleware.ts) — same API, renamed to reflect its purpose.
- Auth: NextAuth.js v5 (beta) self-hosted, DB session strategy (PrismaAdapter), no Clerk/Auth0.
- Proxy uses `auth(...)` higher-order export from `@/lib/auth` to attach req.auth; protected path list maintained in proxy.ts.

## Session Notes

- Using Next.js 16.2.4 with React 19 and Tailwind CSS v4.
- Using Bun.
- shadcn version 4.5.0 was used; it auto-detected Tailwind v4.
- lucide-react ^1.11.0 installed as a direct dependency.
- Custom authentication implemented with NextAuth v5 (no 3rd-party services like Clerk/Auth0).
- Prisma 7.8.0 — generated client goes to app/generated/prisma/; import `PrismaClient` from `@/app/generated/prisma/client`. Constructor requires `{ adapter }`. @prisma/adapter-pg used for Postgres/Supabase connections.
- prisma.config.ts holds the datasource URL (Prisma 7 removed `url` from schema); loads .env via `dotenv/config`.
- Generated Prisma client uses ESM (`moduleFormat = "esm"`); `previewFeatures` removed in 7.8 (queryCompiler/driverAdapters are stable).
- AUTH_SECRET generated locally (32-byte base64) and set in .env. AUTH_URL set to `http://localhost:3000`.
- OAuth credentials are placeholders — user must fill in.
